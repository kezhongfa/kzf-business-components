import { Styles } from "jss";
import { formWrapper as fw } from "../../../../style/index.jss";

export const wrapper: Styles = {
  ...fw,
};
export const itemWrapper: Styles = {
  display: "inline-flex",
  alignItems: "center",
};

export const timePicker: Styles = {
  width: "100px",
};
