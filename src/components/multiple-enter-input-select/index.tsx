import React, { forwardRef } from "react";
import MultipleEnterInputSelect from "./multiple-enter-input-select";

export default forwardRef((props, ref) => <MultipleEnterInputSelect forwardRef={ref} {...props} />);
