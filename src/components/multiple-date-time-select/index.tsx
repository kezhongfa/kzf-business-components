import React, { forwardRef } from "react";
import MultipleDateTimeSelect from "./multiple-date-time-select";

export default forwardRef((props, ref) => <MultipleDateTimeSelect forwardRef={ref} {...props} />);
